---
name: demo-yaml
description: >
  Format the final output as a single valid YAML document. Use proactively when the user asks for "YAML output",
  "yaml format", "demo yaml", or invokes the demo-presenter command and wants YAML.
license: MIT
---

# Demo YAML Output

Format the final output as a single valid YAML document.

- Start with `---`.
- 2-space indentation.
- Lists use `- `.
- No prose around the YAML, no markdown fences.
