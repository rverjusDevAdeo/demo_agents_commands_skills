---
name: demo-json
description: >
  Format the final output as a single valid JSON object. Use proactively when the user asks for "JSON output",
  "json format", "demo json", or invokes the demo-presenter command and wants JSON.
license: MIT
---

# Demo JSON Output

Format the final output as a single valid JSON object.

- Double quotes around all strings.
- 2-space indentation.
- No trailing commas.
- No prose around the JSON, no markdown fences.
